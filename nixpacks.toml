# Nixpacks configuration for Railway deployment (root directory fallback)
[phases.setup]
nixPkgs = ["jdk21", "maven"]

[phases.build]
cmds = ["cd backend && mvn clean package -DskipTests"]

[start]
cmd = "cd backend && java -Dserver.port=$PORT -jar target/vigilai-backend-0.1.0.jar"
